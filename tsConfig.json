{
    "compilerOptions": {
      "target": "ES6",               // Ensure target is ES6 or higher
      "experimentalDecorators": true, // Enable decorators
      "emitDecoratorMetadata": true,  // Emit design-type metadata for decorators
      "moduleResolution": "node",     // Proper module resolution
      "strict": true                  // Optional: To enable strict type-checking
    }
  }
  